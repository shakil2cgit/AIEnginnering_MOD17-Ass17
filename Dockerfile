# ==============================================================================
# ধাপ ১: বেস ইমেজ সিলেক্ট করা (Base Image Selection)
# ==============================================================================
FROM python:3.10-slim

# ==============================================================================
# ধাপ ২: এনভাইরনমেন্ট ভ্যারিয়েবল ও ওয়ার্কিং ডিরেক্টরি সেট করা (Environment & Workdir)
# ==============================================================================
ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1

WORKDIR /app

# ==============================================================================
# ধাপ ৩: সিস্টেম ডিপেন্ডেন্সি ইনস্টল করা (Install System Dependencies for OpenCV)
# ==============================================================================
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1 \
    libglib2.0-0 \
    libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# ==============================================================================
# ধাপ ৪: পাইথন প্যাকেজ ইনস্টল করা (Install Python Dependencies)
# ==============================================================================
COPY requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

# ==============================================================================
# ধাপ ৫: প্রজেক্ট সোর্স কোড ও মডেল কপি করা (Copy Application Code & Models)
# ==============================================================================
COPY app /app/app
COPY models /app/models

# ==============================================================================
# ধাপ ৬: পোর্ট এক্সপোজ এবং অ্যাপ্লিকেশন রান কমান্ড (Expose Port & Run Command)
# ==============================================================================
EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
